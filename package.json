{
  "name": "unified-agent-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Centralized AI agent platform for monitoring and responding across Email, WhatsApp, Instagram, and Discord",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:electron": "cd frontend && npm run electron:dev",
    "dev:backend": "cd backend && uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:whatsapp": "cd whatsapp-bridge && npm run dev",
    "build": "cd frontend && npm run build && npm run electron:build",
    "install:all": "npm install && cd frontend && npm install && cd ../whatsapp-bridge && npm install"
  }
}
